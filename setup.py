from setuptools import setup

setup(
   name='Snow-Drone',
   version='0.0.1',
   description='Image acquisition and online processing for drone snowflake data collection.',
   author='',
   author_email='',
   packages=['Snow-Drone'],
   install_requires=['wheel', 'opencv-python', 'numpy', 'flirpy'],
)